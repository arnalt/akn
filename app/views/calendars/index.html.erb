<% provide(:title, 'Calendars') %>
<div class="row">
  <div class="offset1">
    <h3><%= t("text.calendar_title") %></h3><br>
    <table class="table table-condensed">
      <tr>
        <th>Name</th>
        <th><%= t("text.work_index_describ") %></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

      <% @calendars.each do |calendar| %>
          <tr>
            <td><%= calendar.datum %></td>
            <td><%= calendar.arbeitstag %></td>
          </tr>
      <% end %>
    </table>
    <br/>
  </div>
</div>